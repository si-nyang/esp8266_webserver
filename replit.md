# Flask 웹서버

## 프로젝트 개요
Python Flask 프레임워크를 사용하여 만든 기본 웹 애플리케이션입니다. 간단하고 깔끔한 디자인의 웹사이트로 홈페이지, 소개 페이지, 에러 핸들링 기능을 포함하고 있습니다.

## 최근 변경사항
- **2025-10-28**: 초기 프로젝트 생성
  - Flask 웹서버 기본 구조 설정
  - 홈페이지, 소개 페이지, 404 에러 페이지 구현
  - 반응형 CSS 디자인 적용
  - 워크플로우 설정 완료
  - 보안 강화: SESSION_SECRET 필수 설정, 디버그 모드 환경 변수 제어

## 프로젝트 구조
```
.
├── app.py                  # Flask 애플리케이션 메인 파일
├── templates/              # HTML 템플릿 디렉토리
│   ├── index.html         # 홈페이지
│   ├── about.html         # 소개 페이지
│   └── 404.html           # 404 에러 페이지
├── static/                 # 정적 파일 디렉토리
│   └── css/
│       └── style.css      # 스타일시트
└── replit.md              # 프로젝트 문서
```

## 기술 스택
- **Backend**: Python 3.11, Flask 3.1
- **Frontend**: HTML5, CSS3
- **템플릿 엔진**: Jinja2

## 주요 기능
1. **라우팅**: 
   - `/` - 홈페이지
   - `/about` - 소개 페이지
   - 404 에러 핸들링

2. **템플릿 렌더링**: Jinja2를 사용한 동적 HTML 생성

3. **정적 파일 서빙**: CSS, JavaScript 등의 정적 파일 지원

4. **반응형 디자인**: 모바일, 태블릿, 데스크톱 모든 화면 크기에 대응

## 실행 방법
서버는 자동으로 실행됩니다. 수동으로 실행하려면:
```bash
python app.py
```

서버는 `http://0.0.0.0:5000`에서 실행됩니다.

## 환경 변수
- `SESSION_SECRET`: Flask 세션 암호화 키 (필수 - 설정되지 않으면 서버가 시작되지 않음)
- `FLASK_DEBUG`: 디버그 모드 활성화 (선택 - true/1/t로 설정 시 활성화, 기본값: false)

## 보안
- SESSION_SECRET이 설정되지 않으면 서버가 시작되지 않도록 구성
- 디버그 모드는 기본적으로 비활성화 (프로덕션 환경 보호)
- 환경 변수를 통한 안전한 설정 관리

## 다음 단계 제안
- RESTful API 엔드포인트 추가
- 데이터베이스 연동 (PostgreSQL)
- 사용자 인증 시스템
- 폼 데이터 처리 기능
- 추가 페이지 및 기능 확장
